{
  "name": "toui-skill",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "AI agent skills + MCP connector for toui — the short-link service embedded in your AI workflow",
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "toui-js": "^0.1.0"
  }
}
